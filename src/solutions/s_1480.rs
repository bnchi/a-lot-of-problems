fn solution(input: Vec<i32>) -> Vec<i32> {
    vec![]
}

#[cfg(test)]
mod test_1480 {
    use super::*;

    #[test]
    fn inital_test() {
        let solution = solution(vec![1, 2, 3, 4]);
        assert_eq!(1, 1);
    }
}
